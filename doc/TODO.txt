This is a TODO list with improvements and new features;

* Start Check and Checkmate implementation;
* GetRow(), GetCol(), GetDiagonals(), GetJump() to each piece based on their moveset (GetVision on interface);
* Add a GetPos() to each piece;